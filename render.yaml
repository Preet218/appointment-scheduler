services:
  - type: web
    name: appointment-scheduler
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false   # you'll paste this value manually in the Render dashboard
